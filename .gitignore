# Python
__pycache__/
*.pyc
.venv/

# Editors / OS
.idea/
.vscode/
.DS_Store

# Local secrets. `.env` venv ile calisirken, `.env.docker` compose ile;
# ikisi de parola tasiyor, ikisi de commit edilmez. Ornek dosyalari
# (.env.example / .env.docker.example) commit'li ve parolasiz.
.env
.env.docker

# CatBoost training scratch output
catboost_info/
notebooks/catboost_info/

# Regenerated every run, not source. The tracked source of truth for "what
# the current model scores" is saved_models/model_meta.json.
data/daily_alerts.csv
metrics/train_metrics_*.json

# NOTE: data/*.csv and saved_models/ ARE committed on purpose so the project
# runs immediately after `git clone` (see README "Quickstart").
